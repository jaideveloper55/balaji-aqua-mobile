// src/lib/company.ts
// TODO: read companyType from the JWT after the Auth module is built.
// One shared switch, so every screen hides WATER_PLANT-only UI the same way.
export const IS_WATER_PLANT = true;
